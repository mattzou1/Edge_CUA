services:
  desktop:
    build: .
    ports:
      - "5900:5900"   # VNC (vncdotool)
      - "6080:80"     # noVNC web UI (nginx inside container listens on 80)
    volumes:
      - ../screenshots:/screenshots
    environment:
      - RESOLUTION=1280x800
    restart: unless-stopped
